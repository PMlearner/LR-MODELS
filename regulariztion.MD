Regularization is a technique used to prevent a model from overfitting by adding a penalty for large weight values. This encourages the model to learn simpler patterns that generalize better to unseen data.


For L2 regularization:

Loss=Prediction Error+λ∑w2


types:
L1(mae),l2(mse)
Dropout

Weight Decay

Early Stopping

Data Augmentation

Label Smoothing

Gradient Clipping

Learning Rate Scheduling

Batch Normalization (not used in LLMs, but general ML)

LoRA Dropout (specific to LoRA finetuning)

Noise Injection (adding small noise to embeddings/weights)