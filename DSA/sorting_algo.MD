Bubble sort:

    lets consider the list,There firt pic first index compare to next if not then change it and check second and third check and change it same to every element 
    Here Biggest element goes last index,next iteration omit the last index
Insertion sort:
    Here in list pic second element and check there before element is corrct else change into the order next select third and second and insert perfect index also check next previous element 

Selection sort:
    Pic first element and fix that as a min index and check if next element is smaller then change then index value as min index same goes untill the final value and give a finalized small index that change to the firt index
Merge Sort:
    Here split list into two equal list and sort those equally and after that sorted two list chekc each value and sort it 
    split and merge will happened
Quick Sort:
    Here list pick any pivot element and check with first if its higher then 